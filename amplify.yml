version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed"
    build:
      commands:
        - echo "Static site"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
